name: Daily n8n Backup (Production Only)

on:
  workflow_dispatch:
    # Cho phép kích hoạt thủ công
  # schedule:
  #   - cron: '15 0 * * *' # 7h15 sáng giờ Việt Nam (UTC+7)

jobs:
  backup:
    if: ${{ github.ref_name == 'master' }}
    runs-on: self-hosted
    # Đặt thời gian timeout cho toàn bộ job này là 30 phút
    timeout-minutes: 30 
    name: Daily n8n Backup (Production Only)
    steps:
      # Bỏ bước checkout code, sử dụng luôn thư mục deploy production
      - name: Thiết lập biến thư mục deploy production
        run: echo "DEPLOY_DIR=deploy_${GITHUB_REF_NAME}" >> $GITHUB_ENV

      - name: Kiểm tra file .env và script backup-n8n.sh
        run: |
          cd $DEPLOY_DIR
          if [ ! -f .env ]; then
            echo "❌ Không tìm thấy file .env trong $DEPLOY_DIR"; exit 1;
          fi
          if [ ! -f backup-n8n.sh ]; then
            echo "❌ Không tìm thấy script backup-n8n.sh trong $DEPLOY_DIR"; exit 1;
          fi

      - name: Thực thi backup
        run: |
          cd $DEPLOY_DIR
          echo "🔒 Đang backup dữ liệu n8n cho môi trường production..."
          bash ./backup-n8n.sh
          echo "✅ Đã backup dữ liệu n8n thành công"

      - name: Gửi thông báo lỗi backup lên Telegram
        if: failure()
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          MESSAGE="❌ Backup n8n thất bại trên branch $GITHUB_REF_NAME lúc $(date '+%Y-%m-%d %H:%M:%S')"
          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d chat_id="${TELEGRAM_CHAT_ID}" \
            -d text="$MESSAGE" 